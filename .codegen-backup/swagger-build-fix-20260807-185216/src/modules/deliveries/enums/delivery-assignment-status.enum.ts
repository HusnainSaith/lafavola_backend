export enum DeliveryAssignmentStatus {
  ASSIGNED = 'assigned',
  ACCEPTED = 'accepted',
  PICKED_UP = 'picked_up',
  EN_ROUTE = 'en_route',
  ARRIVING = 'arriving',
  DELIVERED = 'delivered',
  FAILED = 'failed',
  CANCELLED = 'cancelled',
}
